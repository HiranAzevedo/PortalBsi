<%= form_for(@tag) do |f| %>
  <% if @tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tag.errors.count, "error") %> prohibited this solicitation from being saved:</h2>

      <ul>
      <% @tag.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label "Nome" %><br>
    <%= f.text_field :name %>
  </div>
    <div class="field">
      <%= f.label "Categoria" %><br>
      <%= f.text_field :category %>
    </div>
  <div class="actions">
    <%= f.submit "Salvar" %>
  </div>
<% end %>
